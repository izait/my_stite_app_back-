1.Реализовать сервис CRUD городов
2.Реализовать сервис CRUD улиц
3.Добавать Таблицу в бд(
    users_addresses[
        id,
        user_id,
        city_id,
        street_id
    ]) + Entity,Repository в проекте

4. Реализовать сервис привязки пользователя к адресу(город,улица) [привязать,перенести,удалить]
5.Вынести управление сервисами в API(для роутов использоваеть конфигурацию вместо аннотаций)
    -users(создать,найти по id,сменить имя, удалить)
    -cities(создать,найти по id,сменить имя, удалить)
    -streets(создать,найти по id,сменить имя, удалить)
    -users_addresses(
		-привязать(создать новую запись в таблице users_addresses),
		-перенести(поменять в текущей строке users_addresses улицу И город),
		-удалить (удалить запись из таблицы users_addresses по её ID)

